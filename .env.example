# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=workshop_scheduler
POSTGRES_USER=workshop
POSTGRES_PASSWORD=workshop_secret

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT stub — fixed test signing secret, local dev only, do not use in production
JWT_SECRET=test-jwt-secret-for-local-dev-only

# Ports
API_GATEWAY_PORT=3000

# gRPC (used from Phase 1 onward)
SCHEDULER_GRPC_HOST=localhost
SCHEDULER_GRPC_PORT=5000
